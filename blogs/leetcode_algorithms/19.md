# <u>Custom Sorting Comparator</u>
<!-- -->
### **👤 By Arshdeep**  &emsp;&emsp;    🕒 February 27, 2025  

---
<br>


## Code (Python)
```python
class Solution:
    def sortJumbled(self, mapping: List[int], nums: List[int]) -> List[int]:
        m = {str(i): str(n) for i, n in enumerate(mapping)}
        idx = {n: i for i, n in enumerate(nums)}
        nums.sort(key = lambda x: (int("".join([m[c] for c in str(x)])), idx[x]))
        return nums

class Solution:
    def frequencySort(self, nums: List[int]) -> List[int]:
        f = Counter(nums)
        return sorted(nums, key=lambda x: (f[x], -x))

```
## Reference Problems
- [2191. Sort the Jumbled Numbers](https://leetcode.com/problems/sort-the-jumbled-numbers/)