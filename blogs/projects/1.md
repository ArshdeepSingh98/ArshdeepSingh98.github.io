# <u>House Price Prediction EDA</u>
<!-- https://github.com/ArshdeepSingh98/HousePricePrediction -->
### **👤 By Arshdeep**  &emsp;&emsp;    🕒 February 19, 2025  

---
<br>

Exploratory Data Analysis and Model training to predict property prices using actual data

## Steps Taken
- Analyzed **missing** and **outlier data**
- Used techniques such as **Imputations, Forward/Backward pass, IQR (Interquartile Range)**
- Visualized data using Box Plot, Histograms, Scatter Plot, Word Cloud for description
- **Coorelation** and **p-values** to analyze statistical significance towards target variable
- Used **Named Entity Recognition** and **LLM Agents** to parse description to get additional features
- Normalize and scale data using **Standard Scaler**, **One-hot encoding**, **TF-IDF**
- Utilized **Linear Regression**, **Random Forest Regressor**, **Cat Boost**, and **XG Boost** 
- Measured the results using $R^2$, $MAE$, $MSE$, and $RMSE$. 

## Random Forest - Predicted vs Actual price graph
![Random Forest Results](blogs/attachments/RandomForest-Prediction-vs-Actual-Graph.png)
 
## Random Forest example code
```python
# Imports
from sklearn.metrics import mean_squared_error, r2_score, mean_absolute_error
from sklearn.ensemble import RandomForestRegressor

# Train and test dependent vs target variable 
X_train = train_df.drop(columns=["price"], axis=1)
y_train = train_df['price']
X_test = test_df.drop(columns=["price"], axis=1)
y_test = test_df['price']

# Initialize and train the Random Forest Regressor model
randomforest_model = RandomForestRegressor(n_estimators=100, random_state=42, n_jobs=-1)
randomforest_model.fit(X_train, y_train)

# Make predictions on the test set
randomforest_y_pred = randomforest_model.predict(X_test)

# Calculate evaluation metrics
r2 = r2_score(y_test, randomforest_y_pred)
mae = mean_absolute_error(y_test, randomforest_y_pred)
mse = mean_squared_error(y_test, randomforest_y_pred)
rmse = np.sqrt(mse)
```